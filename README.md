# rctl
rctl is a cross-platform command-line tool for securely connecting to remote machines through a tunnel and executing management tasks such as synchronization, command execution, and system operations.
